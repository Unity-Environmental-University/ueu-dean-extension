# 2026-03-09 Retrospective

## What we did
- Evolved types from `@decohere` JSDoc to three-phase dissolution system (Volatile/Fluid/Salt)
- PII anonymization at parse boundary via SHA-256 (studentName -> studentToken)
- CanvasLink updated for async context + status/incident pills
- Manifest split for Chrome (service_worker) vs Firefox (background.scripts)
- Removed stale generated files (Salt types now live in src/types/records.ts)
- Built course search popup — auto-focused, searches Canvas via cookie auth
- Added keyboard shortcuts: Alt+Shift+F (open search), Alt+Shift+R (reload extension)
- Wired @ueu/canvas-alkahest as local dependency
- Separate dist/chrome and dist/firefox output dirs with watch scripts

## What worked
- The alkahest phase metaphor (Volatile/Fluid/Salt) landed cleanly — it maps naturally to how types actually behave in practice
- PII anonymization at the parse boundary is elegant — SHA-256 hashing means student names never exist outside parsePage()
- Course search popup went from zero to working in Canvas in one session
- Local file deps (file:../alkahest-ts, file:../ueu-canvas-alkahest) keep things movable without publishing overhead

## What was bumpy
- Extension dev experience is rough — Chrome MV3 killed real HMR, so we're on build --watch + manual reload (Alt+Shift+R helps)
- The dist/ vs source folder confusion cost some time — easy to forget when loading unpacked
- Account ID for Canvas search wasn't obvious (403 on account 1), solved by fetching accounts first
- Firefox vs Chrome manifest differences needed a dynamic vite config approach

## Open threads
- ~~Test on an actual Salesforce dishonesty case page~~ — in production use by deans
- alkahest-ts and alkahest-py repos have uncommitted work
- ueu-canvas-alkahest — uncommitted
- sme-rhizome-builder canvas_types.py — uncommitted
- Publishing packages (when ready)
- ~~Firefox testing~~ — works but dropped; reinstall-on-every-update makes distribution a pain, reverting to Chrome-only
